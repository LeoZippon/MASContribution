role: tester
canonical_role: verifier
description: "Runs or designs tests and reports pass/fail evidence; functionally equivalent to verifier for code tasks."
model: "${MODEL_NAME}"
temperature: 0.2
max_tokens: 2048
permissions:
  inherit_from: verifier
prompt_file: "configs/benchmark_specs/prompts/verifier.md"
metadata:
  role_family: verifier
  is_alias: true
