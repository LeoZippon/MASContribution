role: executor
canonical_role: coder
description: "Executes the main task action; functionally equivalent to coder in code-generation workflows."
model: "${MODEL_NAME}"
temperature: 0.2
max_tokens: 2048
permissions:
  inherit_from: coder
prompt_file: "configs/benchmark_specs/prompts/coder.md"
metadata:
  role_family: coder
  is_alias: true
