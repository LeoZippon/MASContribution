role: aggregator
canonical_role: finalizer
description: "Aggregates candidate outputs, votes, critiques, or evidence into a final response."
model: "${MODEL_NAME}"
temperature: 0.2
max_tokens: 2048
permissions:
  inherit_from: finalizer
prompt_file: "configs/benchmark_specs/prompts/finalizer.md"
metadata:
  role_family: finalizer
  is_alias: true