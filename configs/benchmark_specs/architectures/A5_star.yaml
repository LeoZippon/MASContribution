id: A5_star
name: Supervisor-Worker Star
family: centralized_coordination
description: "A star topology where a supervisor mediates communication among specialized workers, then a finalizer produces the final output."

roles:
  - supervisor
  - planner
  - coder
  - verifier
  - critic
  - researcher
  - finalizer

canonical_roles:
  supervisor: supervisor
  planner: planner
  coder: coder
  verifier: verifier
  critic: critic
  researcher: researcher
  finalizer: finalizer

entrypoint: supervisor
terminal_nodes:
  - final_answer

edges:
  - [supervisor, planner]
  - [planner, supervisor]
  - [supervisor, researcher]
  - [researcher, supervisor]
  - [supervisor, coder]
  - [coder, supervisor]
  - [supervisor, verifier]
  - [verifier, supervisor]
  - [supervisor, critic]
  - [critic, supervisor]
  - [supervisor, finalizer]
  - [finalizer, final_answer]

orchestration:
  execution_mode: star
  routing_policy: supervisor_mediated
  aggregation_policy: supervisor_to_finalizer
  stopping_condition: finalizer_emits_executable_answer
  max_rounds: 2

default_permissions:
  supervisor: supervisor
  planner: planner
  researcher: researcher
  coder: coder
  verifier: verifier
  critic: critic
  finalizer: finalizer

recommended_datasets:
  - humaneval
  - mbpp
  - teambench
  - multiagentbench
  - marble

attribution_notes:
  removal_protocols: [hard_removal, null_agent_replacement, weak_model_replacement]
  primary_questions:
    - "Does high degree make the supervisor genuinely high contribution?"
    - "How much contribution comes from routing authority and final authority?"
    - "Can task dependencies remain plan-code-check while communication is star-shaped?"

metadata:
  node_count: 7
  topology_type: star
  role_preserving_rewire_compatible: true
  paper_label: A5
